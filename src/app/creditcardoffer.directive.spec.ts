import { CreditcardofferDirective } from './creditcardoffer.directive';

describe('CreditcardofferDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditcardofferDirective();
    expect(directive).toBeTruthy();
  });
});
